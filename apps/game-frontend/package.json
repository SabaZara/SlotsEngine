{
  "name": "@slots-engine/game-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@slots-engine/shared-types": "*"
  },
  "devDependencies": {
    "gsap": "^3.15.0",
    "pixi.js": "^8.19.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
