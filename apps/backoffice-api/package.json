{
  "name": "@slots-engine/backoffice-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@slots-engine/logging": "*",
    "@slots-engine/math-engine": "*",
    "@slots-engine/mongo-schemas": "*",
    "@slots-engine/rng": "*",
    "@slots-engine/shared-types": "*",
    "fastify": "^5.2.0",
    "mongodb": "^6.12.0"
  }
}
